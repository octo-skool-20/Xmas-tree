# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Build on merge / push
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - run: npm i && npm run-script build